# Server Dashboard Backend Configuration

# Frontend origin for CORS
FRONTEND_ORIGIN=https://vvicier-nextcloud.minmi-gar.ts.net:3000

# Server port and host
PORT=3001
HOST=0.0.0.0

# Path to game servers directory
# This should point to where your Minecraft server files are accessible
# (can be a local path, NFS mount, or SMB mount)
GAME_SERVERS_PATH=/home/vvicier/gameservers

# Minecraft RCON configuration
# Set these to point to your Minecraft server
RCON_HOST=localhost
RCON_PORT=25575
RCON_PASSWORD=2106

# Optional: TLS certificate paths (for Tailscale or custom certs)
# TLS_CERT_PATH=/etc/tailscale-certs/your-host.ts.net.crt
# TLS_KEY_PATH=/etc/tailscale-certs/your-host.ts.net.key

# Systemd configuration
SYSTEMD_USER_DIR=/etc/systemd/system

# Network interface to monitor for stats
NETWORK_INTERFACE=eno1
